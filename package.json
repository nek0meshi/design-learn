{
  "name": "design-learn",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cp node_modules/normalize.css/normalize.css public/css",
    "build:watch": "npm run build && npm run sass:watch",
    "sass:watch": "sass --watch scss/style.scss public/css/style.css"
  },
  "devDependencies": {
    "normalize.css": "^8.0.1",
    "sass": "^1.42.1"
  }
}
